## 알고리즘

### 정렬 알고리즘
#### 버블 정렬(Bubble sort)
- 최대 $\frac{n(n-1)}{2}$ 번 정렬
- 한 번 돌때마다 마지막 하나가 정렬되므로 원소들이 거품올라오는 것처럼 보여 버블 정렬이다.
- 거의 모든 상황에서 최악의 성능. 단, 이미 정렬된 자료에서는 1번만 돌면 되기 때문에 최선의 성능을 보여준다.
#### 선택 정렬(Selection sort)
- 정렬이 어떻게 되어 있든 일관성 있게 $\frac{n(n-1)}{2}$에 비례하는 시간이 걸린다. 또한, 버블 정렬보다 두 배 정도 빠르다.
##### 이중 선택 정렬(Double selection sort)
- 끝까지 훑어서 최솟값과 최댓값을 동시에 찾아낸 뒤 최솟값은 1번째와 바꾸고 최댓값은 끝과 바꾼 다음 훑는 범위를 양쪽으로 한 칸씩 줄여서 반복하는 방식
- 이 방법을 쓰면 반복 횟수가 반으로 줄어든다.
#### 삽입 정렬(Insertion sort)
- k번째 원소를 1부터 k-1까지와 비교해 적절한 위치에 끼워넣고 그 뒤의 자료를 한 칸씩 뒤로 밀어낸다는 아이디어.
- 실제 구현 시에는 (k-1)번째 원소부터 비교를 시작하여 비교 결과에 따라 즉시 뒤로 밀어내다가 적절한 위치를 찾으면 끼워넣는 방식으로 만든다.
- 적절한 위치를 찾으면 그 즉시 루프가 종료된다는 점으로 인해 무식하게 배열을 빙빙 도는 다른 $O(n^2)$알고리즘에 비해 평균적으로 빠르다. 특히, 배열이 거의 정렬이 되어있다면 삽입할 위치를 찾는 과정이 빠르게 종료되므로 $O(n)$에 근접하는 성능을 내는 것이 가능하다. 따라서 다른 빠른 정렬을 사용하여 거시적인 정렬을 마친 뒤 삽입 정렬로 마무리하는 방식도 많이 사용된다.
- 이미 정렬되어 있는 자료구조에 자료를 하나씩 삽입/제거하는 경우에는 현실적으로 최고의 정렬 알고리즘이 되는데, 탐색을 제외한 오버헤드가 매우 적기 대문이다.
- 배열의 크기가 작은 경우 역시 상당히 효율적이다. 일반적으로 빠르다고 알려진 알고리즘들도 배열이 작은 경우에는 대부분 삽입 정렬에 밀린다. 따라서 고성능 알고리즘들 중에서는 배열의 사이즈가 클 때는 $O(n \log n)$ 알고리즘을 쓰다가 정렬해야 할 부분이 작을 때는 삽입 정렬로 전환하는 것들도 있다.
- 다만 자료구조에 따라선 뒤로 밀어내는데 걸리는 시간이 클 수도 있으며, 이런 자료에서 앞의 예시처럼 작은 게 뒤쪽에 몰려있다면(내림차순의 경우 큰 게 뒤쪽에 몰려있으면) 그야말로 헬게이트다.
#### 병합 정렬(Merge sort)
#### 힙 정렬(Heap sort)
#### 퀵 정렬(Quick sort)
#### 트리 정렬(Tree sort)
#### 기수 정렬(Radix sort)
#### 계수 정렬(Counting sort)
#### 팀 정렬(Tim sort)
#### 블록 병합 정렬(Block merge sort)
#### 인트로 정렬(Intro sort)


데이터베이스 인덱싱이란?  
정렬 알고리즘 아는대로 말해주시겠어요?  
주소창에 'google.com'을 치면 접속되는 과정 설명

1. 새벽에 고객사가 로그인이 안된다고 하면 어떻게할건가?  
2. 해야되는 일이 있는데 다른사람이 급하게 해야된다고 일을 부탁하면 어떡할건가?  
3. 이력서에 썼던 프로젝트에서 데이터 흐름을 그려보세요

캐싱에 관하여 시스템 전반부 및 설계에 대하여 물어봄  
난이도가 조금 있었음

1. 멀티스레드와 싱글스레드의 장단점  
2. Css in js와 css전처리기의 비교  
3. 부정적에너지를 지닌 사람과 협업할때 어떤태도를 갖추겠는가

스레드는 무엇이며, 싱글스레드와 멀티스레드의 장단점은?  
맵과 셋에 대해 설명하시오.  
제일 힘들었던 프로젝트와 그 이유는?

1. 지원 동기  
2. 기술적 질문  
3. 알고리즘 문제 풀이 설명

라이브코테 3문제(크게 어렵진 않음)  
이전에 경험한 서비스 아키텍처 설명해주세요

여태까지 본인이 한 프로젝트에 대한 설명 그리고 태클이 들어오면 개선점을 말해야한다

프로세스와 스레드?  
병렬성 과 동시성?  
트리의 시간 복잡도

업무 투입시 oo업무를 며칠 안에 끝낼 수 있는지, 남들이 나에 대한 평가를 할 때 어떻게 대처할 것인지 등

아키텍쳐 선택의 이유?  
개발이 아닌 기획 관점에서 본인이 기여한 경험이 있는지?  
본인의 일이 아닌데 나서서 해결한 경험이 있는지?

스프링과 관련된 질문  
자료 구조와 관련된 질문  
JVM에 관련된 질문

1. 스프링 클래스 코드리뷰  
2. 예외처리 방법  
3. Http 메서드 종류

- 시간복잡도 관련 질문  
- 락 방식 차이  
- 프로젝트 하면서 가장 잘한 업무

ArrayList와 LinkedList의 차이와 어떨때 쓰면 좋은지!

1. 멀티 인스턴스의 사이드 이펙트 설명 및 해결 방법 논의  
2. 디자인 선택 사이드 이펙트 및 선택 사유  
3. 어려운 경험에 대한 선택과 행동

4. 프로젝트에서 어려웠던 기술적 도전  
5. 인덱스에 대한 설명  
6. 어려웠던 기술적 도전에 대한 상세한 설명

7. 특정 상황을 제시하고 기술적으로 어떻게 해결할 것인지 물어봄  
8. 상사와 후임간 갈등이 있을 때 어떻게 대처할 것인지?  
9. 업무 중 다른 팀에서 업무 요청이 들어 왔을 때. 어떻게 해결할 것인지?

- 이전회사에서 가장 어려웠던 경험, 문제를 어떻게 해결했는지  
- 전반적으로, 이전에 담당한 서비스에 대한 오너십을 얼마나 가지고 일했는지를 물어보는 느낌이었음  
- 기술면접 시, 이력 바탕으로 기술선택 및 해결과정에 대해 상세히 물어봄

- 동료와 협업하는 태도  
- 특정 상황에 대한 대처 방법