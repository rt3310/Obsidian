## v-text
요소의 텍스트 콘텐츠를 업데이트 한다.

### 기대값
`string`
### 세부사항
`v-text`는 요소의 textContent 속성을 설정하여 동작하므로, 요소 내부의 기존 콘텐츠를 모두 덮어쓴다.
`textContent`의 일부만 업데이트해야 한다면 mastache 보간법을 대신 사용해야 한다.
### 예시
```vue
<span v-text="msg"></span>
<!-- 아래와 동일 -->
<span>{{msg}}</span>
```

## v-html

